import { EvidenziaricercaDirective } from './evidenziaricerca.directive';

describe('EvidenziaricercaDirective', () => {
  it('should create an instance', () => {
    const directive = new EvidenziaricercaDirective();
    expect(directive).toBeTruthy();
  });
});
