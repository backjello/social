import { RiducitestoPipe } from './riducitesto.pipe';

describe('RiducitestoPipe', () => {
  it('create an instance', () => {
    const pipe = new RiducitestoPipe();
    expect(pipe).toBeTruthy();
  });
});
